{
  "name": "gomoku-web",
  "version": "2.8.1",
  "private": true,
  "type": "module",
  "description": "Local-first Gomoku game, analysis lab, variation tree and adaptive training workspace.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "node scripts/lint-architecture.mjs",
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build-standalone.mjs",
    "smoke": "node scripts/smoke-browser.mjs",
    "check": "npm run lint && npm test && npm run build && npm run smoke"
  }
}
