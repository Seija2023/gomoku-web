{
  "name": "gomoku-web",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build-standalone.mjs",
    "check": "npm test && npm run build"
  }
}
