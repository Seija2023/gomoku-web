{
  "name": "gomoku-web",
  "version": "2.8.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build-standalone.mjs",
    "smoke": "node scripts/smoke-browser.mjs",
    "check": "npm test && npm run build && npm run smoke"
  }
}
